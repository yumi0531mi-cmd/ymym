# Streamlit 배포 점검 기록 — 2026-08-17 KST

- 원격 `main` 브랜치에 `f9d44a0` (`Use 5-minute swing highs for T1 targets`)를 반영했다.
- 앱 URL: https://a6qpcbmie4eddngnrs2vb5.streamlit.app/
- 사용자 브라우저에서 초기 접근 후 대기 상태를 두 차례 재확인했다.
- 관찰 시점에는 Streamlit 페이지가 로딩 표시와 `Manage app`만 표시했으며, 앱 본문 또는 오류 메시지는 아직 렌더링되지 않았다.
- 아직 종목 분석 버튼을 누르지 않았으므로 KIS 시세·분봉 API 요청을 추가로 발생시키지 않았다.

다음 확인: 재배포 완료 후 앱의 초기 안내 문구에 `1차·2차 목표는 완료 5분봉 구조를 우선 반영합니다`가 나타나는지 확인한다.

앱 관리 로그 패널을 열어 `main` 브랜치와 `yumi0531mi-cmd/ymym/main/app.py` 배포 대상을 확인했다. 다만 로그 본문은 아직 비어 있어, 이 화면만으로는 새 커밋 적용 완료 또는 런타임 오류 여부를 판정할 수 없었다.

관리 패널을 닫은 뒤에도 앱 본문은 로딩 표시 상태로 남아 있었다. 아직 오류 문구나 초기 안내 문구가 표시되지 않았으며, 종목 분석을 실행하지 않았다.

임베드 보기에서도 원격 앱 본문은 계속 로딩 표시 상태였으며, 확인 시점까지 초기 안내 문구는 렌더링되지 않았다. 반면 동일 커밋을 로컬에서 12초간 headless Streamlit으로 기동한 결과 서버가 정상 시작했고 Python 초기화 오류는 나타나지 않았다. 따라서 현재 원격 상태는 코드 초기화 실패가 아니라 Streamlit Cloud의 재배포·세션 로딩 지연 가능성이 높지만, 화면 확인 전에는 배포 완료로 판정하지 않는다.

2026-08-17 12:40 UTC 재확인에서도 원격 앱은 임베드 보기에서 로딩 표시만 보였다. 원격 화면의 정상 렌더링은 아직 확인하지 못했다.

사용자 확인 후 Streamlit 관리 화면에서 앱 재부팅 확인 버튼을 실행했다. 재부팅 완료·화면 정상 렌더링 여부는 다음 점검에서 확인한다.

재부팅 후 약 30초 뒤 앱 URL을 다시 열었으나 본문은 여전히 로딩 표시 상태였다. 관리 로그 패널에는 배포 대상(`main`, `yumi0531mi-cmd/ymym/main/app.py`)만 표시되고 시작 로그나 오류 로그는 나타나지 않았다.

Streamlit 대시보드에서 `ymym · main · app.py` 앱이 계정의 배포 목록에 남아 있고, 관리 메뉴에서 재부팅·분석·설정 항목을 제공하는 것을 확인했다. 현재 대시보드 목록 자체에는 마지막 커밋 SHA나 빌드 완료 상태가 표시되지 않았다.

원격 `/_stcore/health` 경로는 인증 리디렉션을 반환했지만, `https://a6qpcbmie4eddngnrs2vb5.streamlit.app/healthz`는 `200 {"status":"ok"}`를 반환했다. 즉 Streamlit 앱 프로세스는 재부팅 후 응답 중이다. 다만 실제 초기 화면 렌더링은 이 브라우저에서 계속 확인하지 못했다.

무한 로딩 원인 진단을 위해 Streamlit 대시보드에서 메인 앱의 관리 메뉴를 열었다. 메뉴에는 설정 항목이 있으며, 다음 단계에서 공개 범위와 배포 설정을 읽기 전용으로 점검한다.

앱 설정의 Sharing 탭을 읽기 전용으로 확인한 결과, 메인 앱은 `This app is public and searchable` 상태다. 따라서 무한 로딩의 원인은 앱 공개 범위나 조회 권한이 아니다.

호출 보호 커밋 `0c0048c`를 원격 main 브랜치에 반영한 뒤 재접속했으나, 사용자 브라우저와 동일하게 앱 본문은 여전히 로딩 스피너 상태였다. 이 변경은 초기 화면에서 KIS 요청을 만들지 않으므로, 현재 로딩은 KIS 토큰·시세 호출 때문이 아니다.

전종목 후보 스크리너 커밋 `7c43f44` 배포 후에도 My Browser에서 앱 본문은 로딩 스피너 상태였다. 로컬 Streamlit 초기 화면 스모크 테스트와 37개 단위 테스트는 통과했다. 새 전종목 후보 검색은 사용자가 버튼을 누르기 전에는 KIS 요청을 하지 않으므로, 이 증상은 해당 후보 검색 호출과 무관하다.

최종 재확인에서 Streamlit 앱 초기 화면이 정상 렌더링됐다. 왼쪽 설정 패널의 `고정 유동성 시작목록 검색`, `전종목 후보 검색`, `선택 종목 분석` 버튼과 60초 자동 새로고침 최소 간격이 확인됐다. 이 초기 화면 확인에서는 KIS 종목 분석·순위 검색 버튼을 누르지 않아 추가 시세 API 호출은 발생하지 않았다.

캐시 버전 수정 커밋 `9aabd40` 재배포 후 초기 화면이 다시 정상 렌더링됐다. `전종목 후보 검색` 버튼도 표시됐다. 버튼 실행은 KIS 순위 API 호출을 발생시키므로, 사용자 토큰이 누락된 현재 상태에서는 추가 호출을 하지 않았다. 모의 응답 기반 통합 테스트에서는 전종목 후보 버튼과 결과 선택 상자가 정상 렌더링됐으며 전체 38개 테스트가 통과했다.

사용자 화면에서 `KIS_ACCESS_TOKEN` 누락 오류가 재현됐다. 원인은 KISClient가 Streamlit `cache_resource`에 생성 당시 Secrets 상태로 보관돼, 서버 프로세스가 남아 있는 상황에서 수동 토큰을 수정해도 기존 인스턴스를 재사용할 가능성이 있는 구조였다. 앱은 이제 KIS 앱키·시크릿·토큰·기본 URL·자동발급 플래그의 SHA-256 지문을 캐시 키에 포함한다. 지문은 비밀값을 화면·로그에 쓰지 않으며, 토큰 변경 시 새 KISClient 인스턴스를 생성한다. 최상위 Secrets와 `[kis]`/`[KIS]` 중첩 섹션도 모두 읽는다. 실제 토큰·시세 호출 없이 40개 테스트와 Streamlit 초기 화면 스모크 테스트를 통과했다.

Secrets 지문 기반 KISClient 캐시 수정 커밋 `27e2f2f` 재배포 후 초기 화면이 정상 렌더링됐다. 전종목 후보 검색 버튼은 표시되지만, 실제 클릭은 KIS 순위 요청을 발생시키므로 토큰 전달 상태를 재검증하려고 누르지 않았다.

사용자 화면 단순화 변경: 토큰을 앱이 읽지 못하면 `고정 유동성 시작목록 검색`, `전종목 후보 검색`, `선택 종목 분석` 버튼을 비활성화하고 한국어 안내만 표시하도록 변경했다. 수동 토큰 별칭(`KIS_TOKEN`, `ACCESS_TOKEN`, `TOKEN`, `access_token`)도 지원한다. 실제 시세 요청 없이 토큰 미설정 버튼 비활성화와 모의 토큰 연결 상태의 전종목 후보 결과 렌더링을 테스트했고, 전체 41개 테스트와 초기 화면 스모크 테스트를 통과했다.

사용자 친화 안내 커밋 `d56898c` 재배포 후 초기 화면을 확인했다. 토큰 미설정 상태에서 `한국투자증권 연결: 아직 준비 중입니다`라는 한국어 안내와 비활성화된 세 검색 버튼이 정상 표시된다. 이 화면 확인에는 KIS 시세·순위 API 호출이 발생하지 않았다.

후보 데이터 모델 안정성 수정 커밋 `7a811df` 재배포 후 초기 화면을 재확인했다. 이전 관리 로그의 NoneType 오류 화면은 현재 앱 본문에 표시되지 않았고, 토큰 미설정 상태의 쉬운 한국어 안내와 비활성화된 검색 버튼만 정상 표시된다. 확인 과정에서 KIS API 요청은 실행하지 않았다.

관리 로그를 확인한 결과, `AttributeError: 'NoneType' object has no attribute '__dict__'`의 직접 원인은 `scanner.universe.UniverseItem`에 적용된 `@dataclass(frozen=True, slots=True)`였다. Streamlit Cloud의 코드 갱신 중 모듈을 다시 불러오는 과정에서 Python 3.12 dataclass가 해당 slotted class의 모듈 정보를 찾지 못한 과거 실행 기록이 남았다. `UniverseItem`도 slots 없이 immutable dataclass로 변경했으며 전체 41개 테스트와 초기 화면 검증을 통과했다. 이후 로그에는 이 오류 화면이 사용자 본문에 나타나지 않는다.

사용자 승인으로 Streamlit 앱을 재시작했다. 재시작 후 초기 화면이 정상 렌더링됐고, 이전의 내부 dataclass 오류 화면은 본문에 다시 나타나지 않았다. 화면에는 토큰 미설정 상태용 한국어 안내와 비활성화된 검색 버튼만 표시된다. 초기 화면 확인에서는 KIS API 요청을 실행하지 않았다.

모바일 카드형 스캐너 커밋 `be80bf8` 재배포 뒤 초기 화면을 확인했다. 다크 카드형 디자인, 접힌 상태의 설정 패널, `반복단타 후보 카드` 제목, 연결 대기 안내와 비활성화된 후보 검색 버튼이 정상 렌더링됐다. KIS 토큰이 현재 전달되지 않아 실시간 가격 카드는 생성하지 않았으며, 모의 KIS 응답 기반 통합 테스트에서는 전종목 후보 선택부터 진입·목표·손절 카드 렌더링까지 통과했다.

밝은 반응형 카드 커밋 `74e1d7b` 재배포 뒤 웹 화면을 확인했다. 배경은 흰색으로 전환됐고, 밝은 설정 패널·연결 대기 안내·후보 카드 안내가 정상 표시됐다. CSS는 700px 이하에서 카드 1열, 그 이상에서 최소 360px 기반 자동 다열을 사용하도록 반영됐다. 연결 토큰이 없는 상태이므로 실제 시세 카드 생성은 실행하지 않았다.

안전한 KIS Secrets 진단 커밋 `ea9831d` 재배포 뒤 초기 화면을 확인했다. 비밀값은 화면·로그에 표시하지 않으며, 연결이 준비되지 않았을 때 검색 버튼은 비활성화된 상태로 유지됐다. 새 진단은 앱키·앱 시크릿·당일 토큰의 확인 여부와 저장 위치 분류만 표시하도록 구현했고, 전체 회귀 테스트 44개를 통과했다. 현재 배포 화면에서는 KIS 연결 대기 상태가 계속 표시되어, 실제 Streamlit Secrets 런타임에서 필요한 키 중 하나 이상을 읽지 못하고 있음을 확인했다.

커밋 `636cf4d` 재배포 뒤 화면 확인에서 앱이 실행 중 오류 화면으로 전환된 것을 발견했다. 화면의 안전한 Traceback은 `/mount/src/ymym/app.py` 308행 `kis_connected = client.ready`에서 예외가 난다는 위치만 제공하며, 원문 예외는 데이터 보호를 위해 Streamlit Cloud가 가렸다. 앱이 비밀값을 표시하지 않으면서도 이 예외를 잡아 연결 대기 상태로 안전하게 계속 렌더링하도록 보완이 필요하다.

앱 관리 로그의 전체 예외를 확인했다. 원인은 KIS Secrets 값이나 KIS API 호출이 아니라, Streamlit `cache_resource`가 이전 코드의 KISClient 인스턴스를 유지한 상태에서 새 코드가 `ready` 속성을 조회해 발생한 `AttributeError`다. 따라서 클라이언트 캐시 버전을 올리고, 이전 인스턴스가 감지되면 캐시를 안전하게 비운 뒤 새 읽기 전용 클라이언트를 다시 만드는 방어 처리가 필요하다. 이 조치는 토큰 발급·시세·주문 요청을 만들지 않는다.

커밋 `a880bf0` 재배포 뒤 오류 화면이 사라지고 앱 초기 화면이 정상 렌더링됐다. 안전 진단 결과는 `앱 키: 확인됨 · 앱 시크릿: 확인됨 · 당일 토큰: 미확인 · 저장 위치: 최상위 Secrets`였다. 즉 Streamlit Secrets 자체는 전달되고 있으며, 현재 문제는 토큰 항목 하나가 앱이 지원하는 이름으로 읽히지 않는 것이다. 확인 과정에서 KIS 시세·순위 API 호출, 토큰 발급, 주문 요청은 하지 않았다.

사용자가 선택한 간단 자동 방식으로 KISClient를 변경했다. KIS 앱 키·앱 시크릿이 있는 경우 앱의 첫 화면에서는 토큰을 발급하지 않고 `자동 발급 대기`로 표시한다. 사용자가 전종목 후보 검색이나 입력 종목 카드 만들기를 시작해 최초 KIS 요청이 필요할 때만 `/oauth2/tokenP` 발급 요청을 한 번 수행한다. 발급 토큰은 앱의 비공개 임시 파일에 최소 권한으로 저장하고, 만료 전에는 재배포 뒤에도 재사용한다. 자동 주문은 추가하지 않았다. 모의 응답으로 발급 요청 1회·이후 재사용·재시작 뒤 재사용·명시적 자동 발급 차단을 검증했고 전체 45개 테스트와 초기 화면 스모크 테스트를 통과했다. 실제 KIS 토큰 발급·시세·주문 요청은 수행하지 않았다.

커밋 `ca60ad9` 반영 뒤 초기 화면을 시세 요청 없이 확인했다. 화면에는 이전의 `당일 토큰: 미확인` 안내와 비활성화된 검색 버튼이 남아 있어, 최신 코드가 아직 Streamlit 인스턴스에 반영되지 않았거나 이전 세션이 남아 있는 것으로 보인다. 실제 토큰 발급·시세·주문 요청은 하지 않았다. 다음 점검에서는 관리 로그에서 최신 커밋 반영 시점을 확인한다.

관리 로그에는 15:21:35 코드 갱신 시작과 15:21:37 앱 갱신 완료가 표시됐으나, 열린 브라우저 화면에는 이전 연결 대기 문구가 남아 있었다. 관리 로그의 현재 화면에는 새 자동 발급 코드에 해당하는 오류가 보이지 않았고, 앞선 KISClient 캐시 오류 기록만 남아 있다. 배포와 브라우저 세션 반영의 시간차를 고려해 새 세션에서 다시 확인한다.

새 브라우저 세션에서 커밋 `ca60ad9`의 최신 배포를 확인했다. 앱은 `한국투자증권 연결 준비가 끝났습니다`와 `첫 검색을 누를 때 오늘 토큰을 1회 자동 발급합니다` 안내를 표시하며, 전종목 후보 찾기 버튼이 활성화됐다. 이 초기 화면 확인에서는 버튼을 누르지 않았으므로 실제 KIS 토큰 발급·시세·주문 요청은 발생하지 않았다.

사용자 요청에 따라 버튼·후보 선택·종목코드 입력을 기본 흐름에서 제거하고, 앱을 열면 국내 또는 미국 시장 순위에서 상위 최대 3개 후보를 자동 선택해 카드로 표시하는 `5.3-live-dashboard`로 변경했다. 각 카드에는 현재가, 등락률, 진입 기준가, 완료된 5분봉 기반 1차·2차 목표, Soft/Hard Stop, 신호·위험 상태·유동성 지표를 즉시 표시한다. 현재가는 60초, 완료 1분봉 구조는 10분, 1호가는 15분, 시장 순위는 2시간 단위 캐시로 분리해 5시간 KIS 호출 예산 1,100건 안에서 최대 3카드 자동 갱신을 목표로 했다. 화면을 열면 최초 시장 순위 요청이 필요하므로 수동 토큰이 없을 때는 그 시점에만 일일 토큰 자동 발급을 한 번 시도한다. 자동 주문은 추가하지 않았다. 모의 KIS 응답 기반 자동 카드 통합 테스트와 전체 45개 회귀 테스트, 초기 화면 스모크 테스트를 통과했다.

사용자 확인 후 최신 실시간 대시보드를 실제 실행했다. 화면은 새 `실시간 반복단타 후보` 레이아웃과 자동 갱신 설정을 표시했고, 호출 보호는 1분 4/30·5시간 16/1100으로 유지됐다. 그러나 화면에는 `실시간 후보를 가져오지 못했습니다` 오류가 표시되어 후보 카드가 생성되지 않았다. 즉 자동 토큰/요청 경로는 일부 실행됐지만 국내 시장 순위 요청 중 오류가 발생한 것으로 보인다. 실제 주문은 실행하지 않았다. 다음 단계에서 관리 로그의 정확한 KIS 오류를 확인한다.

관리 로그를 열어 확인했으나 화면에 노출된 로그 구간은 과거 재배포 오류와 최신 앱 갱신 완료 시점까지만 보여, 현재 후보 조회 실패의 KIS 응답 본문은 확인되지 않았다. 오류 화면은 예외 세부값을 사용자에게 숨기도록 되어 있으므로, 다음 수정에서는 후보 순위 실패 시 유동성 시작목록을 자동 대체해 카드가 비어 있지 않도록 하고, 안전한 오류 종류만 화면에 표시하도록 보완한다.

실제 실행에서 시장 순위 단계가 실패해 카드가 비었던 문제를 보완했다. 이제 시장 순위 API가 실패하거나 빈 결과를 반환하면, 국내·미국의 사전 정의된 유동성 시작목록을 현재 등락률·거래대금 기준으로 자동 정렬해 상위 3개 카드를 즉시 분석한다. 화면에는 `유동성 시작목록 자동 대체`라고 명시해 전종목 시장 순위와 혼동하지 않게 했다. 여러 후보의 상세 차트도 각 카드별 고유 식별자를 사용하도록 수정했다. 전체 46개 회귀 테스트와 초기 화면 스모크 테스트를 통과했고 실제 KIS 추가 요청은 만들지 않았다.

사용자 요구에 맞춰 화면을 `5.4-mixed-live-scanner`로 전환했다. 자동 후보는 상승 추세(그다음 박스권)를 먼저 보이도록 정렬하고, 0.5~5% 폭의 반복 구조가 있는 경우에만 `반복단타 가능`을 추가 표시한다. 따라서 반복단타 조건이 없더라도 상승 추세의 진입·목표·손절 카드는 첫 화면에 남는다. HTML 카드 일부가 텍스트로 보이던 문제를 피하기 위해 현재가·진입 기준가·1차/2차 목표·Soft/Hard Stop을 Streamlit 기본 카드 요소로 렌더링하도록 바꿨다. 별도 관심 종목 검색은 국내 한글명 또는 6자리 코드와 미국 티커를 지원하며, 최신 KRX 상장목록에서 만든 2,793개 한글명·코드 색인을 앱에 포함했다. `현대차` 한글 검색이 005380 카드로 추가되는 흐름까지 포함해 전체 47개 회귀 테스트와 초기 화면 스모크 테스트를 통과했다. 실제 KIS 추가 요청은 만들지 않았다.

커밋 `1a3f5e7`의 실제 배포 화면을 확인했다. 제목은 `실시간 상승·반복단타 혼합 스캐너`로 바뀌었고, 왼쪽에 `관심 종목 바로 보기` 입력칸이 있으며 국내 한글명·6자리 코드 및 미국 티커 예시를 표시한다. 카드 HTML이 텍스트로 보이던 오류는 사라졌고, 005380 현대차 카드에 현재가·신호·진입·1차/2차 목표·Soft/Hard Stop이 각각 읽기 쉬운 기본 카드로 표시됐다. 당일 장 외 시간이라 완료 1분봉이 부족해 매매 레벨은 `조건 미확인`으로 표기됐으며, 이는 가격을 임의로 만들지 않는 안전 동작이다. 입력 검증 중 화면 자동 갱신으로 첫 좌표 입력이 무효화되어 최신 화면을 다시 확인한 뒤 진행한다.

최신 화면을 다시 확인해 혼합형 제목, 한글·티커 검색 입력 안내, 유동성 자동 대체 출처, 현재가와 매매 레벨 카드의 정상 렌더링을 재확인했다. Streamlit의 60초 자동 갱신 때문에 좌표 기반 직접 입력은 화면 버전이 바뀔 때 무효화될 수 있어, 실제 입력 검증은 모의 KIS 응답 통합 테스트로 이미 수행한 흐름을 근거로 한다. 사용자 브라우저에서 추가 시세 요청을 반복하지 않는다.
