# v6.11 실패 표본 감사 작업 노트

## 화면에서 확인한 고정 사실

- 감사 버전은 `6.11-isolated-boundary-capture`이며, 완료 표본은 27건이다. 세 시간대 방향을 모두 맞힌 표본은 4건이고 하나 이상 방향에 실패한 표본은 23건이다.
- 사용자가 지정한 20건 성능 스냅샷은 5분 25.0%, 15분 20.0%, 30분 25.0%였다. 감사 화면의 27건은 같은 버전에 후속 완료된 표본이 더해진 원문 집합이다. 분석에서 20건 성능 수치와 27건 감사 원문을 혼용하지 않는다.
- v6.11은 신호 시점의 `Direction Engine` 상세와 시간축별 입력 지표를 저장하지 않아 감사 표의 해당 필드는 5·15·30분 모두 `미기록 27`로 나타난다. 따라서 v6.11 저장 원문만으로는 당시 지표의 실제 수치·원점수·후속 방향 변환을 재구성할 수 없다.
- 실패 원문 상단에는 `US_PRE` 세션의 SKHY·SPCX·INTC·TQQQ·AAOI가 반복되어 있으며, `WATCH · NONE · 장세 전환 대기`, `WATCH · NONE · 하락 구조`, `WATCH · RANGE_SWING · 박스 하단 평균회귀`, `REVERSAL_MEAN_REVERSION · RANGE_SWING`이 보인다.

## 감사 원칙

- v6.11의 실측 성능 실패는 수정 필요성을 뒷받침하지만, 저장하지 않은 신호 시점 입력을 사후 추정해 원인으로 단정하지 않는다.
- 이후 수정은 v6.11의 반복된 구조·시간축 준비 문제와 현재 코드의 재현 가능한 계산 흐름을 대조해 코드상 확정할 수 있는 오류·중복·시간축 불일치에만 한정한다.
- 수정 버전은 현재 v6.11과 입력 상세를 분리 저장하고, 국내장 신규 표본 20건 이상으로 독립 검증한다.

## 국내장 이력 확보 확인 대기

v6.18 국내 수집 화면은 완료 1분봉 119개와 경로 준비 `False`를 표시했다. 이는 15분 30개와 30분 20개 완료봉 요건을 충족할 수 없는 이력 길이였으며, 성능 표본을 만들지 않는 것이 정상 동작이다. KIS 공식 국내 일별분봉 샘플은 한 호출 최대 120건이지만 `FID_INPUT_DATE_1`과 `FID_INPUT_HOUR_1`로 과거 날짜·시간 분봉을 조회할 수 있다고 명시한다. 따라서 v6.19는 이 공식 파라미터를 사용해 가장 이른 행 직전 시각부터 최대 6페이지를 연속 조회한다.

v6.19 공개 국내 수집 화면은 재배포 직후 후보 자동 분석을 시작했고, 호출 보호는 1분 6/30·5시간 6/1100으로 표시됐다. 아직 후보 분석 진행 중이므로 완료 1분봉 수·경로 준비 상태·신규 표본 시작 여부는 분석 완료 후 다시 확인해야 한다.

v6.20은 장 시작 전 커서가 반복되는 경우를 방지하기 위해 직전 날짜 15:30으로 넘기고, 동일 시각 페이지가 다시 오면 즉시 중단하도록 배포했다. 재배포 직후 공개 국내 화면은 다시 후보 분석 진행 상태였고 호출 보호는 1분 7/30·5시간 7/1100이었다. 아직 최종 카드와 표본 시작 결과가 렌더링되지 않아, 실제 KIS가 이전 거래일 행을 반환했는지는 화면 분석이 끝난 뒤에만 판정한다.

30초 추가 대기 뒤에도 화면은 동일한 후보 분석 진행 상태와 1분 7/30·5시간 7/1100 표시를 유지했다. 이 상태만으로는 요청이 정상적으로 이전 거래일 이력을 조회 중인지, 특정 KIS 호출이 대기·재시도 중인지는 구분할 수 없다. 따라서 완료 화면이나 명시적 오류가 나오기 전에는 실제 720개 이력 확보 성공으로 기록하지 않는다.

최대 응답 대기 후 v6.20 화면은 후보 분석을 완료했고, `005930` 1건의 고정 예측 표본을 실제로 시작했다. 화면은 5분 상승·15분 상승·30분 박스 방향을 표시하고 5·15·30분 실제가 경계를 각각 대기 상태로 표시했다. 이는 적어도 한 후보에서 15·30분 경로 준비가 성공했음을 뜻한다. 완료 표본 수는 아직 0이므로 방향 적중률이나 수정 효과는 판정하지 않는다.

v6.20의 15분·30분 경계 대기 중 Supabase 영속 조회 예외가 화면을 중단시킨 사실을 확인했다. v6.21은 이전 재배포 세대의 `PersistenceError`도 비치명적 저장소 오류로 분류하고, EventStore·CycleStore 캐시 계약을 새로 분리해 재생성한다. 배포 직후 국내 화면은 오류 화면 없이 후보 분석을 재개했지만, 표본 경계 완료 전에는 수집 안정성 통과로 판정하지 않는다.

v6.21 공개 국내 화면은 오류 화면 없이 3개 동시 표본을 표시했고, 가장 이른 `005930`의 5분 실제가는 다시 `수집 완료`였다. 15분·30분은 각각 대기 상태이며 완료 30분 표본은 0건이다. 따라서 저장소 오류가 첫 5분 경계를 통과한 것은 확인됐지만, 모든 시간대 수집 안정성 판정은 30분 경계 완료 후에만 한다.

이후 수집 안정성 우선 모드는 대기 표본 5/5에 도달해 카드 재분석을 멈추고 5·15·30분 실제가 경계 저장을 우선했다. 가장 이른 표본의 5분 실제가는 계속 수집 완료였고, 나머지 네 표본은 신호 시각 순서대로 5분 대기 상태였다. 이 화면에는 DATA_MISSING이 표시되지 않았으며, 15분과 30분 완료 전이므로 성능 평가는 보류한다.

다음 확인 시에는 5개 표본 모두 5분 실제가가 수집 완료였고, 가장 이른 `005930`의 15분 실제가도 수집 완료였다. 나머지 15분과 다섯 표본의 30분 경계는 대기 상태였다. 영속 조회 오류 화면은 재발하지 않았고 DATA_MISSING 표시도 없었다.

v6.21의 첫 30분 실제가가 `수집 완료`인데도 완료 채점 대기로 남았다. 원인은 경계 REST fallback이 카드용 5분 캐시를 재사용해 경계와 다른 시각의 KIS 현재가를 보존할 수 있던 설계였다. v6.22는 경계용 비캐시 현재가 조회를 별도 경로로 분리했다. 재배포 직후 국내 화면은 오류 없이 후보 분석을 다시 시작했으며, 새 독립 표본이 시작된 뒤 5·15·30분 실제가와 완료 채점 상태를 다시 확인해야 한다.

v6.22 국내 화면은 새 버전 표본을 생성했고 첫 `005930`의 5분 실제가가 수집 완료로 표시됐다. 이후 대기 표본이 5/5로 채워졌으며, 첫 표본은 15분·30분 대기, 나머지는 신호 시각 순서대로 5분 대기였다. 이 화면에는 DATA_MISSING이나 영속 저장소 오류가 표시되지 않았고, 30분 완료·채점 전이므로 성능 집계는 보류한다.

v6.22의 첫 30분 경계는 `수집 완료` 뒤 완료 채점되어 `1/20`으로 집계됐고 대기 표에서는 사라졌다. 이 결과는 비캐시 경계 현재가와 영속 저장소 오류 보호가 한 표본의 5·15·30분 수집부터 사후 채점까지 연결된다는 초기 확인이다. 단일 완료 표본은 성능 분석 표본으로 충분하지 않다.

후속 v6.22 배치에서는 5개 표본의 5분 실제가가 수집 완료였고, 가장 이른 두 표본의 15분 실제가도 수집 완료였다. 나머지 15분·30분 경계는 시각 순서대로 대기 중이며, 화면에는 DATA_MISSING과 영속 저장소 오류가 보이지 않았다. 완료 표본은 여전히 1/20이므로 방향 성능을 해석하지 않는다.

v6.23은 카드 본체에서 FORECAST_AUDIT을 직접 생성하지 않고, 대기 최대 5건의 별도 백그라운드 배치만 고정 예측을 생성하도록 분리했다. 또한 같은 카드의 공통 전략 엔진을 표본 100건 전 비결정적 calibration 구간에서는 한 번만 실행하고, 숨은 카드 재분석 fragment를 제거했다. 일반 국내 화면 재배포 직후 후보 검색이 시작됐으며, 이 시점의 호출 보호는 1분 8/30·5시간 8/1100이고 경계 수집 예약은 없었다. 카드 표시·실시간 갱신은 후보 분석 완료 후 실제 장중에서 추가 확인이 필요하다.

일반 v6.23 국내 화면은 검증 진행 표본 1건이 별도로 표시되는 동안 후보풀 23종목·빠른 선별 14종목·정밀 분석 8종목을 완료했고, 상승 조건을 모두 통과한 FINAL_BUY가 없을 때에도 관찰 후보 5종목 카드를 계속 표시했다. 첫 관찰 카드에는 `PRICE_LEVEL_WAIT`와 실제 계산값·요구 기준, 5·15·30분 방향, 매매 유형이 유지됐다. 이는 검증 대기 때문에 후보 검색이나 관찰 카드 표시가 중단되지 않는 초기 장중 확인이다.

이후 v6.23 일반 국내 화면에는 대기 고정 표본 5건과 `DATA_MISSING` 2건이 표시됐다. 한 표본은 15분 REST fallback이 KIS 호출 예산 보호에 의해 실패했고, 다른 표본은 5분 경계 수집 창(+75초) 경과 뒤 REST fallback을 시도한 것으로 표시됐다. 같은 시점에 화면은 KIS WebSocket 구독의 `ALREADY IN USE` 재연결 오류를 표시했다. 이 두 건은 성능 통계에서 제외되며, 본체 카드 품질과 별도로 수집 안정성 원인으로 기록한다.

카드 우선 배치 뒤 일반 국내 화면은 후보풀 23종목·빠른 선별 15종목·정밀 분석 8종목·1차 목표 가능 0종목·FINAL_BUY 0종목·관찰 후보 5종목을 표시했다. 첫 관찰 카드 `069500 KODEX 200`은 현재가 108,250원 부근에서 `MINUTE_DATA_WAIT`였고, 완료 1분봉 599개로 5·15·30분 경로 준비가 `False`라고 명시됐다. 이는 전략 관문 탈락이 아니라 30분 고차봉 20개에 필요한 완료 분봉 수가 한 개 부족한 데이터 준비 상태다. 해당 카드는 매수 보류로 분류됐으며, 이 한 건만으로 필터를 완화하지 않는다.

90초 뒤 재확인에서도 동일 카드가 완료 1분봉 599개·경로 준비 False 상태에 머물렀다. 따라서 단순 1분 경계 대기 문제가 아니라 국내 분봉 연속 조회가 599개까지만 반환되는 반복 경로 또는 최신 완료봉 병합 문제를 코드와 실제 KIS 응답 기준으로 추가 점검해야 한다.

최신 1페이지 완료분봉 병합 배포 뒤 초기 후보 분석은 여전히 약 5분 뒤에 완료됐다. 완료 화면에서는 `069500 KODEX 200`이 `MINUTE_DATA_WAIT`를 벗어나 `PRICE_LEVEL_WAIT`로 전환됐고, 현재가 107,840원 부근, 5분 박스·15분 상승·30분 박스 방향과 15·30분 구조 확인, 손절거리 0.64%가 표시됐다. 즉 5·15·30분 경로 준비 부족은 해소됐지만, 매수가·1차 목표·손절 중 일부가 구조 가격대 규칙을 만족하지 않아 매수 보류가 됐다. 이는 방향·가격 수준 불일치를 숨기지 않는 정상 관찰 분류이며, 8개 후보의 냉간 분봉 분석 시간이 여전히 본체 UX 병목이다.

냉간 분봉 분석 UX 병목은 전략 조건이나 후보 수를 줄이지 않고 처리했다. 일반 본체는 8개 정밀 후보를 순서대로 동일하게 분석하되, 각 후보의 이력 분석이 끝나는 즉시 우선순위 상위 5개 준비 카드를 화면에 미리 표시한다. 전체 분석이 끝나면 기존 FINAL_BUY·진입대기·관찰 섹션으로 다시 분류한다. 이 변경은 후보 선택·가격 계산·전략 관문·검증기 구조를 바꾸지 않으며, 전체 자동검사 155개와 앱 회귀 테스트 35개를 통과했다. 실제 Streamlit 장중에서 미리보기 렌더링 시점은 배포 후 추가 확인이 필요하다.

점진 카드 미리보기 배포 후 일반 국내 화면은 정밀 분석 완료 8/8종목으로 전환됐고, 후보풀 23종목·빠른 선별 14종목·정밀 분석 8종목·1차 목표 가능 0종목·FINAL_BUY 0종목·관찰 후보 5종목을 표시했다. 첫 관찰 카드 `069500 KODEX 200`은 `RR_FAIL`로 분류됐고 비용 반영 손익비 0.32, 요구 기준 1.10 이상, 5분 박스·15분 상승·30분 박스, 손절거리 0.34%, 1차 목표 여유 +0.26%가 표시됐다. 이는 방향은 혼합이지만 15·30분 구조가 하방이 아니고, 가격 구조의 순손익비가 기준을 못 넘겨 매수 보류된 사례다. 실제 후보 수나 손익비 기준은 이 한 관찰만으로 변경하지 않는다.

같은 화면에서 백그라운드 검증은 카드와 분리된 상태로 `대기 표본 5/5건`, 이번 분 시작 0건, 순환 1/23으로 표시됐다. 호출 계측은 1분 3/30, 5시간 3/1100, 후보검색 2, 신규 검증표본 0, 카드 현재가 1, 분봉 조회 0, 경계 수집 0, WebSocket 승인·재연결 0이었다. 즉 이 관찰 결과는 본체의 실제 후보 분석이며, 검증 표본 수집이 후보 분석을 막거나 호출 예산을 대량 사용한 흔적은 이 실행 화면에서 보이지 않았다.

관찰 후보 가격·방향 교차 점검 표를 추가한 `3e02727` 배포 후에도 국내 화면은 후보풀 23종목·빠른 선별 14종목·정밀 분석 8종목·1차 목표 가능 0종목·FINAL_BUY 0종목·관찰 후보 5종목으로 동일하게 표시됐다. 첫 카드 `396500 TIGER 반도체TOP10`은 현재가 35,690원, `RR_FAIL`, 비용 반영 손익비 0.39 대 요구 기준 1.10 이상, 5분 박스·15분 상승·30분 박스, 1차 목표 여유 +0.36%로 표시됐다. 이는 동일 사유의 두 번째 독립 화면 관찰이지만, 현재까지는 단일 유형의 관찰 표본 두 건뿐이므로 손익비 계산이나 관문을 변경하지 않는다. 새 교차 점검 표는 카드 목록 뒤에 렌더링되도록 배포됐고, 현재 뷰포트에는 첫 카드까지만 보여 전체 표의 실제 행은 추가 화면 확인이 필요하다.

모바일·웹의 즉시 비교 목적에 맞게 교차 점검 표는 이후 개별 관찰 카드보다 먼저 렌더링하도록 조정했다. 표는 현재가, 매수가/1·2차 목표, Soft/Hard Stop, 구조, 5·15·30분 방향, 각 시간대 예상 범위, 주요 탈락 사유를 한 행에 표시하며, 완료 차트 가격 구조가 확정되지 않은 값은 `구조 재확인`으로 표시해 추천가로 오인하지 않게 한다. 이 UI 변경은 전략·필터·검증기 구조를 바꾸지 않았고, 앱 회귀 37개와 전체 자동검사 157개를 통과했다.

`12c863e` 배포 후 국내 장중 화면에서 교차 점검 표가 개별 카드보다 먼저 실제로 렌더링됐다. 표의 상단 5행은 `396500 TIGER 반도체TOP10` 현재가 35,930원, `0167A0 SOL AI반도체TOP2플러스` 17,670원, `102110 TIGER 200` 107,840원, `229200 KODEX 코스닥150` 14,575원, `005930 삼성전자` 267,750원이었다. 이 중 396500과 102110은 매수가·1·2차 목표가의 수치 구조가 표시됐고, 0167A0·229200은 `구조 재확인`으로 표시돼 미확정 가격을 추천가로 노출하지 않았다. 해당 화면의 퍼널은 후보풀 23종목·빠른 선별 14종목·정밀 분석 8종목·1차 목표 가능 0종목·FINAL_BUY 0종목·관찰 후보 5종목으로 유지됐다. 표의 수평 열은 모바일 화면에서 일부 잘리므로 각 행의 5·15·30분 범위·Soft/Hard Stop·탈락 사유는 개별 카드에서도 계속 확인해야 하며, 현재 구현은 Streamlit 데이터표의 가로 스크롤을 사용한다.

정밀 분석 8종목의 관찰 탈락 관문별 건수는 기존 상세 영역에만 있던 집계를 공통 함수로 분리해 후보 퍼널 바로 아래에도 표시하도록 조정했다. 이 표는 실제 관찰 사유 문자열과 종목 수만 집계하며, `MINUTE_DATA_WAIT` 같은 데이터 준비 상태와 `RR_FAIL`·`PRICE_LEVEL_WAIT` 같은 전략·가격 관문을 같은 실패로 합산하지 않는다. 상세 영역도 동일 공통 함수를 사용하므로 두 영역의 수치가 분기되지 않는다. 이 변경은 전략 임계값과 검증기 구조를 바꾸지 않았고, 앱 회귀 38개·전체 자동검사 158개를 통과했다.

`f181ce4` 배포 후 국내 장중 화면은 후보풀 23종목·빠른 선별 14종목·정밀 분석 8종목·1차 목표 가능 0종목·현재 1회 진입 0종목·관찰 후보 5종목을 다시 표시했다. 후보 퍼널 아래에 탈락 관문 집계 표 컨테이너가 렌더링됐으나 첫 화면 캡처에는 행 내용이 보이지 않았다. 따라서 이 상태에서는 관찰 후보 5종목과 집계 함수의 테스트 통과는 확인됐지만, 실제 빈 표가 데이터 없음인지 Streamlit 표 렌더링 지연인지 다음 화면 갱신에서 구분해야 하며 집계 수치를 단정하지 않는다.

20초 뒤 동일 국내 화면에서 탈락 관문 표의 실제 행이 렌더링됐다. 정밀 분석 8종목 기준 `구조 가격대 재확인` 5종목, `데이터·방향 계산 대기` 3종목으로 집계됐다. 두 행의 합계가 8인 것은 화면에 최대 5개만 노출하는 관찰 후보 카드 수와 다른 정밀 분석 전체 기준 집계이기 때문이다. 따라서 이번 실행의 FINAL_BUY 0은 반복매매 조건이나 최소 점수의 단일 차단이 아니라, 5종목의 구조 가격대 확정 부족과 3종목의 데이터·방향 준비 부족으로 설명된다. 이 실행 한 번의 구성만으로 가격 구조 규칙이나 데이터 기준을 완화하지 않는다.

관문 집계의 초기 표기는 카드의 `PRICE_LEVEL_WAIT`·`MINUTE_DATA_WAIT` 같은 구체 코드가 아니라 사람이 읽는 사유만 보여, 카드와 집계를 바로 대조하기 어려웠다. 이는 전략 결과가 아니라 보고 경로의 분류 불일치이므로 `candidate_stage_summary()`가 `observation_diagnostic()`의 실제 관문 코드와 사유를 함께 집계하도록 정렬했다. 예를 들어 손익비 미달은 `RR_FAIL · 비용 반영 손익비 부족`, 분봉·경로 준비 부족은 `MINUTE_DATA_WAIT · KIS 완료 1분봉·방향 경로 재수신 대기`로 카드와 같은 계약을 사용한다. 이 보정은 관문 순서·기준·계산을 바꾸지 않았으며 앱 회귀 39개와 전체 자동검사 159개를 통과했다.

코드 정렬 배포 뒤 다음 국내 실행에서도 정밀 분석 8종목의 관찰 관문 집계가 `PRICE_LEVEL_WAIT · 구조 가격대 재확인` 6종목, `MINUTE_DATA_WAIT · KIS 완료 1분봉·방향 경로 재수신 대기` 2종목으로 표시됐다. 앞선 실행의 구조 가격대 5·데이터 준비 3과 방향은 같고 구성만 1종목 이동했으므로, 가격 구조 미확정이 단일 카드의 우연한 관찰이 아니라 현재 본체의 반복 병목 후보임은 확인됐다. 단, `PRICE_LEVEL_WAIT`는 상방 가격 경로 미확정 또는 손절<Soft Stop<진입<1차<2차 목표 순서 미성립 모두를 포괄하므로, 실제 원시 가격 값과 각 조건의 불일치를 카드별로 추적하기 전에는 전략 규칙·임계값을 변경하지 않는다.

반복 `PRICE_LEVEL_WAIT`의 원인을 다음 장중 카드에서 그대로 확인할 수 있도록 진단을 확장했다. 가격 구조가 추천 가능하지 않을 때에는 상방 가격 경로 확인 여부, `Hard < Soft < 진입 < 1차 < 2차` 실제 가격 순서, 순서 일치 여부, 현재가 대비 진입가가 +0.2% 이내인지가 `실제값 / 요구식`으로 표시된다. 이 진단은 미확정 값을 매수가·목표가로 노출하지 않는 기존 안전 규칙을 유지하며, 카드 요약에는 여전히 `구조 재확인`을 표시한다. 전략의 손익비·목표·손절 기준은 변경하지 않았고 앱 회귀 40개·전체 자동검사 160개를 통과했다.
